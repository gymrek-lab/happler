channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - conda-forge::python=3.9 # the lowest version of python that we formally support; keep in sync with release.yml, tests.yml, .readthedocs.yaml, and noxfile.py
  - conda-forge::pip==25.0.1
  - conda-forge::poetry==2.1.1 # should keep this in sync with version in release.yml
  - conda-forge::nox==2025.2.9
  - conda-forge::nox-poetry==1.1.0
  - conda-forge::poetry-conda==0.1.4
  - conda-forge::poetry-plugin-export==1.9.0
  - pip:
    - poetry-plugin-shell==1.0.1
